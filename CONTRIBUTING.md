# 贡献指南

提交问题时附上插件与 Obsidian 版本、操作系统、主题、复现步骤、预期/实际结果，以及去除个人信息的最小 Markdown 示例。

开发使用 Node.js 22+，先运行 `npm ci`。修改后按 README 执行类型检查、逻辑测试、浏览器测试和构建。分页改动还需运行 PDF 核对脚本；真实宿主测试请记录应用版本和主题。

不要提交 Vault、data.json、私有文献、访问令牌、node_modules 或生成的测试结果。保留依赖锁文件与第三方许可资料。

Pull Request 应说明问题、最终行为、相关验证及未覆盖的限制。避免在同一改动中混入无关重构。
