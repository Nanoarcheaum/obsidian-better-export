# 0.8.2 · 2026-09-20

- 修复多列图片同时保留单图对齐和宽度标记时出现对角排列；旧笔记无需重写即可并列显示，新排版不再生成冲突标记。
- Live Preview 不再把整条 CodeMirror 行设为网格，避免编辑器语法节点占用图片网格。
- 分组图片新增右下角拖动柄，按原图比例缩放并微调组内占比，松手后写回 Markdown 并同步导出；不再设置固定高度，避免裁切和圆角丢失。
- 插件设置新增“正文图片圆角”，支持 0–32 px，并同步应用到编辑器、阅读视图和 PDF 导出；默认值调整为 10 px。
- 修复“一键对齐”的 4:3 容器仍显示直角背景；容器与图片现在共用圆角设置并正确裁切边缘。
- “导出 PDF”改为原生保存流程：选择路径后直接生成 PDF，不再弹出系统打印窗口；原打印流程保留为独立的“系统打印”按钮。
- 媒体菜单新增“一键对齐”，清除组内手动尺寸并恢复等宽、等高的 4:3 完整显示框。
- 封面支持多张图片，新增本地多选导入和批量粘贴，追加图片不覆盖已有内容。
- 每张图片可设置宽高、左右/上下位置、适配方式、居中和叠放顺序；尺寸以纸张百分比表示，失焦后应用。
- 首次批量导入自动平铺；保留旧单图封面，旧徽标可转成可排版图片。
- 图片数据随模板复制、全局模板、撤销与路径重命名保留；移除布局图片不删除附件。
- 侧栏重建后保留原滚动位置；切换主分页时仍回到该分页顶部。
- 封面行前/行后增加毫米滑杆、页面百分比与纵向标尺；A4 纵向可调到 297 mm，移除原先 UI 与数据层的 80 mm 双重上限。版头也从 80 mm 扩展到 120 mm，旧值最高保留到 400 mm。
- 验证：44 项逻辑测试与 23 项浏览器测试；宿主适配器不替代真实 Obsidian 验收。

# 0.8.1 · 2026-09-05

- 全局模板改为独立可展开面板，版式、封面·版头和引用恢复三个横向分页。
- 项目显示名称统一为 Better Export，作者为 Nanoarcheaum，插件 ID 保持不变。
- 整理公开仓库、安装文档、项目评价、许可证和 CI；补齐版本映射。

# 0.8.0 · 2026-09-05

- 版头/封面支持原生 Markdown 与 LaTeX；等待公式和图片完成后分页。
- 新增粘贴整页图片封面及完整显示/铺满裁切。
- 移除示例入口，增加模板删除、撤销删除和保留文档副本。
- 新增完整导出配置的全局模板，引用规则按文档保存。
- 新增双向链接颜色与链接/文字/源码三种输出。
- 验证：39 项逻辑测试、18 项 Edge 浏览器测试；浏览器使用宿主适配器测试异步公式生命周期，未在真实 Obsidian 中运行 MathJax 或安装至真实 Vault。

# 0.7.0 · 2026-09-05

本次从 0.6.0 开发快照修复，未安装到真实 Obsidian Vault，也未修改旧项目或原交接评估。

## 主要改动

1. 抽出分页模块：长文本与列表续排、表格逐行分页及重复表头、标题与后文协调、显式分页符处理。不能安全容纳的内容阻止输出并说明原因。
2. 统一正文/设置更新，直接读取正在编辑的正文；旧渲染作废，预览失败或待更新时禁止打印；打印通过 afterprint 清理。
3. 抽出引用语法与 CSL 处理：中文键、代码上下文排除、错误定位、作者年份消歧、CSL JSON 作者信息保留；当页不同定位信息保留，引用删除后完整清理。
4. 统一 Vault/Zotero 文献去重与缺失字段补全，改善 BibTeX 识别和 Paper-easy 字段适配。
5. 自动保存本篇版式、处理笔记/目录/模板附件重命名，共享模板修改自动生成本篇副本；模板撤销与隐藏列恢复。
6. 稳定窗口尺寸、最小化/恢复、拖动边界、关闭时的异步任务；增加独立预览缩放、旧预览提示、错误重试和点击定位；媒体样式回显与清除，保护捕获选区。
7. 保存依赖锁文件，随包内置 CSL 样式及语言资源，不在插件运行期间请求引用样式服务器。

## 本次实际验证

- `npm run build`：通过（包含 TypeScript 检查），已生成 0.7.0 main.js。
- `npm test`：35 项逻辑回归全部通过。
- `npm run test:browser`：14 项 Edge 无头浏览器回归全部通过。使用小型 Obsidian 宿主适配器，调用实际面板、状态和分页模块。
- PDF 内容核对：9 页 A4 与浏览器预览页数一致；移除每页页码与空白后，18,004 字符逐字一致，无丢失/重复。脚本：`test/verify-pdf.py`。
- 浏览器输出与截图位于开发目录 `test-results/`，不写入真实 Vault。

这些是本次实际结果，不沿用历史“23 项测试通过”的声明。真实 Obsidian 的 MarkdownRenderer、不同主题、插件组合及系统打印对话框仍需集成试用；浏览器适配器无法证明全部宿主差异均兼容。

## 明确边界

- 当页文献区是按文献和定位信息汇总，不是逐次脚注模型；Chicago 使用第 18 版作者—年份样式。
- 视频输出、超高不可拆分表格行/公式、超出纸张的封面等会提示调整，不会静默裁切。
- 模板仍为通用行列编辑器，没有增加 DOCX 自动转换、合并单元格或单元格图片。
- Markdown 单独复制不会携带插件设置；迁移需同时备份 data.json 和附件。未新增飞书发布、附件搬移或清理。
- Quick Copy 纯文本识别仍应人工核对；非单键引用语法会提示修复。
